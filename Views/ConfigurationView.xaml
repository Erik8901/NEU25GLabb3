<UserControl x:Class="L046_Labb3_Code_Along.Views.ConfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:L046_Labb3_Code_Along.Views"
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
 >
    <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" DataContext="{Binding ConfigurationViewModel}" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="0.5*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
        
            <Grid Grid.Row="0">
                <Label Grid.Column="0" Content="{Binding ActivePack.Name}" FontSize="12" FontWeight="Bold" Margin="10,0,0,0" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button FontSize="10" Width="20" Height="20" Command="{Binding OpenPackOptionsPopUpCommand}">
                        <fa:IconBlock Icon="sliders"/>
                    </Button>
                    <Button FontSize="10" Width="20" Height="20" Margin="6,0,0,0" Command="{Binding AddQuestionCommand}">
                        <fa:IconBlock Icon="plus"/>
                    </Button>
                    <Button FontSize="10" Width="20" Height="20" Margin="6,0,0,0" Command="{Binding RemoveQuestionCommand}">
                        <fa:IconBlock Icon="minus"/>
                    </Button>
                </StackPanel>
            </Grid>

            <ListBox Grid.Row="1" 
                     ItemsSource="{Binding ActivePack.Questions}" 
                     DisplayMemberPath="Query" 
                     SelectedItem="{Binding ActiveQuestion}"
                     VerticalAlignment="Stretch"
                     Margin="0 0 0 10"/>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="Question" FontSize="12" FontWeight="Bold" Margin="20,20,0,0"/>
            <TextBox Grid.Row="1" Margin="20,0,10,0" MinHeight="270" Text="{Binding ActiveQuestion.Query}"/>
            <StackPanel Grid.Row="2" Orientation="Vertical" Margin="20,30,10,0">
                <Label Content="CorrectAnswer:" FontSize="15" FontWeight="Bold"/>
                <TextBox Text="{Binding ActiveQuestion.CorrectAnswer }" FontSize="15"/>
                <Label Content="IncorrectAnswer 1:" FontSize="15" FontWeight="Bold"/>
                <TextBox Text="{Binding ActiveQuestion.IncorrectAnswers[0] }" FontSize="15"/>
                <Label Content="IncorrectAnswer 2:" FontSize="15" FontWeight="Bold"/>
                <TextBox Text="{Binding ActiveQuestion.IncorrectAnswers[1] }" FontSize="15"/>
                <Label Content="IncorrectAnswer 3:" FontSize="15" FontWeight="Bold"/>
                <TextBox Text="{Binding ActiveQuestion.IncorrectAnswers[2] }" FontSize="15"/>
            </StackPanel>
         </Grid>
    </Grid>
</UserControl>