<UserControl x:Class="L046_Labb3_Code_Along.Views.PlayerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:L046_Labb3_Code_Along.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
            
>
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="20"  DataContext="{Binding PlayerViewModel}">
    <Label Content="{Binding TimeRemaining}"
           HorizontalAlignment="Center"
           FontSize="24"
           Margin="0,0,0,20"/>

        <Label Content="{Binding CurrentQuestion.Query}"></Label>
       <TextBlock>
    <TextBlock.Text>
        <MultiBinding StringFormat="Question {0} of {1}">
            <Binding Path="QuestionNumber" />
            <Binding Path="ActivePack.Questions.Count" />
        </MultiBinding>
    </TextBlock.Text>
</TextBlock>

       
        <Grid HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            
            <Button Grid.Row="0" Grid.Column="0"
                Content="{Binding CurrentQuestion.CorrectAnswer}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="40"
                Width="300"
                Margin="10"
                Command="{Binding SubmitAnswerCommand}" 
                CommandParameter="{Binding CurrentQuestion.CorrectAnswer}"/>

            <Button Grid.Row="0" Grid.Column="1"
                Content="{Binding CurrentQuestion.IncorrectAnswers[0]}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="40"
                Width="300"
                Margin="10" 
                Command="{Binding SubmitAnswerCommand}"    
                CommandParameter="{Binding CurrentQuestion.IncorrectAnswers[0]}"/>

           
            <Button Grid.Row="1" Grid.Column="0"
                Content="{Binding CurrentQuestion.IncorrectAnswers[1]}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="40"
                Width="300"
                Margin="10"
                Command="{Binding SubmitAnswerCommand}"    
                CommandParameter="{Binding CurrentQuestion.IncorrectAnswers[1]}"/>

            <Button Grid.Row="1" Grid.Column="1"
                Content="{Binding CurrentQuestion.IncorrectAnswers[2]}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Padding="40"
                Width="300"
                Margin="10"
                 Command="{Binding SubmitAnswerCommand}"    
            CommandParameter="{Binding CurrentQuestion.IncorrectAnswers[1]}"/>
        </Grid>
    </StackPanel>
  
</UserControl>
